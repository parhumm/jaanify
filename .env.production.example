# Jaanify — Production Environment Template
# DO NOT put actual secrets here. Set via CI/CD secrets or platform env vars.

# ── Secrets (set via `gh secret set` or platform dashboard) ───────────
# DATABASE_URL=<set via Railway/Supabase>
# REDIS_URL=<set via Upstash>
# JWT_SECRET=<generate: openssl rand -base64 32>
# REFRESH_TOKEN_SECRET=<generate: openssl rand -base64 32>
# OPENAI_API_KEY=<set via OpenAI dashboard>
# GOOGLE_CLIENT_ID=<set via Google Cloud Console>
# GOOGLE_CLIENT_SECRET=<set via Google Cloud Console>

# ── Non-secret config ─────────────────────────────────────────────────
NODE_ENV=production
PORT=4000
HOST=0.0.0.0
CORS_ORIGIN=https://jaanify.com
NEXT_PUBLIC_API_URL=https://api.jaanify.com
