# Jaanify — Production Environment Template
# NEVER commit this file with actual secrets!
# Set these via your deployment platform's environment variables or secrets manager.

# ── Secrets (set via CI/CD secrets or platform env vars) ──────────────
# DATABASE_URL=<set via Railway/platform secrets>
# REDIS_URL=<set via Upstash dashboard>
# JWT_SECRET=<generate: openssl rand -base64 48>
# REFRESH_TOKEN_SECRET=<generate: openssl rand -base64 48>
# GOOGLE_CLIENT_ID=<from Google Cloud Console>
# GOOGLE_CLIENT_SECRET=<from Google Cloud Console>
# OPENAI_API_KEY=<from OpenAI dashboard>

# ── Non-secret config ─────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=warn
CORS_ORIGIN=https://jaanify.com
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
NEXT_PUBLIC_API_URL=https://api.jaanify.com
